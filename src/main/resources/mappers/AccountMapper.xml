<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mountain.doo.repository.AccountMapper">

<!--    회원정보 저장-->
    <insert id="save">
        INSERT INTO account
        (account_id, password, name, gender, email, phone_no, address)
        VALUES (#{accountId}, #{password}, #{name}, #{gender}, #{email}, #{phoneNo}, #{address})

    </insert>

<!--    마이페이지  회원 정보-->
<!--    <select id="" resultType="Account">-->
<!--        SELECT account_id,password,name,gender,email,phone_no,address-->
<!--        FROM account-->
<!--        WHERE account_id = #{accountId}-->

<!--    </select>-->

<!--    정보수정 dto 불러오기-->
    <select id="searchInfoById" resultType="AccountModifyDTO">
        SELECT password,email,phone_no,address
        FROM account
        WHERE account_id = #{accountId}

    </select>

    <update id="modifyInfo">
        UPDATE account
        SET password = #{password}, email = #{email}, phone_no = #{phoneNo} , address = #{address}
        WHERE account_id = #{accountId}
    </update>

    <delete id="deleteInfo">
        DELETE FROM account
        WHERE account_id = #{accountId}

    </delete>


</mapper>